--------
NOTES
--------

Application gives the best performance with SMT=4. Set SMT=4 before running an application.

Run the following command to run the docker
docker run -it --rm --cap-add=SYS_PTRACE --cap-add=SYS_NICE --shm-size=1G hpc/examinimd:2020.04-dev
